doctype html
html
  head
    meta(charset='UTF-8')
    title Real Time scheduling - demo

  body
    form(action='')
      table
        tr
          th Processi
          th Priorità
            span(class='tooltip')
              | Minore è il valore, maggiore è la priorità del processo.
              | Ignorato in caso di rate-monotonic.
          th T
          th WCET

        each val, index in ['a', 'b', 'c', 'd', 'e']
          tr
            td= val
            td
              input(type='number', min='0', class='priority', value=index)
            td
              input(type='number', min='0', class='period')
            td
              input(type='number', min='0', class='wcet')

        fieldset(class='algorithm-selection')
          ul
          -
            var algorithms = {
              'fps': 'FPS',
              'rate-monotoic': 'FPS: rate monotonic',
              'edf': 'EDF',
            }
          each al_label, algo in algorithms
            -var al_id = 'radio-' + algo
            li
              input(id=al_id, type='radio', name='alghorithm', value=algo)
              label(for=al_id)= al_label

        fieldset(class='options-selection')
          input(id='opt-preemption', type='checkbox', value='preemption')
          label(for='opt-preemption') preemption
